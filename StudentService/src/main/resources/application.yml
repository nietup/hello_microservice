server:
  port: 9002

spring:
  application:
    name: STUDENT-SERVICE

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:9003/eureka
  instance:
    hostname: localhost
